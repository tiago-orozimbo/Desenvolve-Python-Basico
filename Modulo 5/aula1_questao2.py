import random
import math
n = int(input('Digite a quantidade de números aleatórios: '))
soma = 0
for i in range (n):
    n_a = ((random.randint (0,100)))
    soma+= n_a
print((math.sqrt(soma)))
